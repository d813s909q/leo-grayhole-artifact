# Outputs

All pipeline outputs (tables, figures, intermediate CSVs) are written here by
the analysis scripts. This directory intentionally ships empty; follow the
reproduction steps in the repository-root README to regenerate everything.
